import React from 'react';

// This component is no longer used - process indicators are now shown in ProcessSteps
const NavigationDots: React.FC = () => {
  return null;
};

export default NavigationDots;